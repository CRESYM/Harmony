#ifndef ABCD_PARAMETERS_H
#define ABCD_PARAMETERS_H

#include <vector> // Include vector for std::vector
#include <complex> // Include complex for std::complex

// Define a type alias for complex numbers
//using Complex = std::complex<double>;

// Define a type alias for arrays of parameters
//using Parameters_types = std::vector<std::vector<Complex>>;

// Function prototypes
//std::vector<std::vector<Complex>> connect_series(const std::vector<std::vector<Complex>>& a, const std::vector<std::vector<Complex>>& b);
//std::vector<std::vector<Complex>> connect_parallel(const std::vector<std::vector<Complex>>& ABCD1, const std::vector<std::vector<Complex>>& ABCD2);
//std::vector<std::vector<Complex>> closing_impedance(const std::vector<std::vector<Complex>>& ABCD, const std::vector<Complex>& Zt, const std::string& direction = "output");
//std::vector<std::vector<Complex>> transformation_dc(const std::vector<std::vector<Complex>>& ABCD);
//std::vector<std::vector<Complex>> transformation_dq(const std::vector<std::vector<Complex>>& ABCD1, const std::vector<std::vector<Complex>>& ABCD2);
//std::vector<std::vector<Complex>> y_to_abcd(const std::vector<std::vector<Complex>>& Y);
//std::vector<std::vector<Complex>> abcd_to_y(const std::vector<std::vector<Complex>>& ABCD);

#endif // ABCD_PARAMETERS_H