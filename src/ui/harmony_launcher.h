#pragma once

/**
 * @file harmony_launcher.h
 * @brief Interactive Harmony launcher (HarmonyUI).
 */

/** @brief Run the HarmonyUI launcher until the user closes the window. */
int runHarmonyLauncher(int argc, char* argv[]);
